#include "main.h"

/**
 * print_numbers - prints all number from zero to nine
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
	int n;

	for (n = 48; n <= 58; n++)
	{
		_putchar(n);
	}
	_putchar(10);
}
